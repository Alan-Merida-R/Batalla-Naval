#include<stdio.h>
#include<string.h>
#include <stdlib.h>
int main()
{
	printf("");
	gets(Variable);
	//fgets(variable, 2000, stdin);
	//scanf("%",&);
	setbuf(stdin, NULL);
	fflush(stdin);
	return 0;
}
